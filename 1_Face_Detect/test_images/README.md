# 测试图片说明

`test_face_cartoon.jpg` 是用于演示程序流程的示例图片。

为了获得更稳定的检测结果，建议另外放入真实人脸照片，例如：

```text
test_images/test.jpg
```

然后运行：

```bash
python face_detect.py --image test_images/test.jpg --output outputs/result.jpg
```
